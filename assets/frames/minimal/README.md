# Minimal Frame Assets

Place a **minimal, modern frame** and matching background texture in this folder for the minimal admin menu theme.

Suggested files:
- `frame.png` – subtle border.
- `background.jpg` – clean background texture.
- `style.css` – style overrides for the minimal layout.

