# Classic Frame Assets

Place the **classic frame** and matching background textures in this folder for the classic admin menu theme.

Suggested files:
- `frame.png` – transparent PNG of the outer frame.
- `background.jpg` – background texture for the menu interface.
- `style.css` – frame-specific layout tweaks (loaded by the plugin in a future version).

